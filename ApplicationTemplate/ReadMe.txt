1) Run the following command from the console before running the webservice. 

> set FLASK_APP=start.py

2) Run the following to start the web service.

> flask run

3) In a browser navigate to the http://127.0.0.1:5000/index.